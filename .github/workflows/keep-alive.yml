name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase project
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            "https://kzlbuxteyiyhjeaflplo.supabase.co/rest/v1/courses?select=id&limit=1" \
            -H "apikey: sb_publishable_XQnNoFgGOKX-yBR1ACnjhA_QWnOFF83"
